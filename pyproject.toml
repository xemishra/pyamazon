[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"


[project]
name = "pyamazon"
version = "1.0.4"
description = "A lightweight Python library for extracting structured product data from Amazon product pages."
readme = "README.md"
requires-python = ">=3.6"
license = { text = "MIT" }

authors = [
    { name = "Shivanand Mishra" }
]

dependencies = [
    "lxml",
    "requests",
    "beautifulsoup4",
]

classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.6",
    "Programming Language :: Python :: 3.7",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]


[project.urls]
Homepage = "https://github.com/xemishra/pyamazon"
Repository = "https://github.com/xemishra/pyamazon"
Issues = "https://github.com/xemishra/pyamazon/issues"


[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
include = ["pyamazon*"]
